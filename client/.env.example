# Copy this file to .env and fill in the actual value.
# Without VITE_API_URL set, vite.config.js's `new URL(env.VITE_API_URL)` throws
# an opaque error on startup for a fresh clone.

# Base URL of the backend API (e.g. http://localhost:3000 for local dev)
VITE_API_URL=